/**
 * Build: `ng build --configuration=staging`
 * Replace with your staging API endpoint before using in CI/CD.
 */
export const environment = {
  production: true,
  name: 'staging',
  apiUrl: 'https://staging-api.example.com/api'
};
