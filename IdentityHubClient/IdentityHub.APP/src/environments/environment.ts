/**
 * Production: `ng build` (default `production` configuration).
 * Set `apiUrl` to your real API host before publishing.
 */
export const environment = {
  production: true,
  name: 'production',
  /** API origin including `/api`, without trailing slash. */
  apiUrl: 'https://api.example.com/api'
};
