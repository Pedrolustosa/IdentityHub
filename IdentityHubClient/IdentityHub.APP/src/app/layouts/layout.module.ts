import { NgModule } from '@angular/core';

/**
 * Layout components are standalone; this module is a structural placeholder matching the enterprise layout.
 */
@NgModule({})
export class LayoutModule {}
